[SYSTEM CONTEXT & CONSTRAINTS FOR ALL AGENTS - JENOSIZE RECRUITMENT ASSIGNMENT]
1. ARCHITECTURE BOUNDARY:
   - Framework: Next.js App Router (Full-Stack TypeScript)
   - Database: Relational DB (Prisma ORM with PostgreSQL/SQLite)
   - Entities: Users, Companies, Contacts, Leads/Deals, Activities, Messages, AuditLogs
   - Validation: Strict Zod schema validation on all API inputs/outputs.

2. SECURITY & AUDIT COMPLIANCE:
   - State-changing APIs (POST, PUT, PATCH, DELETE) MUST trigger AuditLogger.log().
   - LINE Webhook MUST verify X-Line-Signature using Channel Secret.
   - Idempotency & Retries MUST be handled for LINE inbound events using webhook event IDs.
   - PII/Secrets MUST be redacted/masked before logging or sending to AI providers.

3. QUALITY & TESTING GATEWAYS:
   - Automated Tests: 1 Core CRM Flow, 1 AI Skill Fallback Flow, 1 LINE Webhook Signature/Idempotency Flow.
   - Playwright / Vitest automation passing 100%. Strict TypeScript mode enforced.

4. DELIVERABLE GOVERNANCE:
   - BA Agent -> docs/01-REQUIREMENTS-BA.md
   - SA Agent -> docs/02-ARCHITECTURE-SA.md
   - PM Agent -> docs/03-PROJECT-PLAN-PM.md
   - QA Agent -> docs/04-TEST-REPORT-QA.md
   - DEV Agent -> src/ & skills/crm-copilot/SKILL.md

5. EXECUTION RULES:
   - ZERO ASSUMPTIONS: Follow JD specs strictly. Do not invent unstated features.
   - SEQUENTIAL PIPELINE: BA -> SA -> PM -> DEV -> QA.
   - SEPARATION OF CONCERNS: AI suggestions MUST remain drafts until human approval.
